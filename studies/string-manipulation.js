/* With operators
strings can be assigned to variables with an assignment operator
*/

/*With string methods
	slice()
		Extracts part of an array and returns a new string
		 ex
	toLowerCase()
		Changes a string to lowercase letters 
		Ex
	toUpperCase()
		Changes a string to upper case letters
		Ex
	indexOf()
		Searches an array for a specific item and returns its position
		ex
	endsWith() 
		Returns true if the string ends with characters and false if not
		Ex
	concat() 
		Joins to strings together
	push() 
		Adds an element to the end of an array
	pop() 
		Removes the last element to the end of an array
	unshift()
		Adds an element to the front of an arrary
	shift()
		Removes an element from, the front of an array
	join()
		Used to join elements of an array into a string
	valueOf()
		Returns the primitive value of a number 
	toString()
		Converts a number into a string
	Splice()
		replaces
		
*/