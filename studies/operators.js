/* Assignment operators 
	(=)  //assigns value

	//reassigns value to var after performing calculations
	 (+=)  
	 (-=)  
     (*=)
	 (/=)

*/	
	
/* Arithmetic operators 
		Add (concat) two numbers
	    (+)
    
	Subtract two numbers
	  (-)

    Multiplies two numbers
    	(*) 

	Divides two numbers 
		(/)

	Divide two numbers, yield remainder
		(%)

	Exponentiation
		(**)
*/ 

/* Comparison operators 
	(>) // greater than
	(<) // less than
	(>=) // greater than or equal to
     (<=) // less than or equal to
	(===)  // equal value
	 (!=) // not equal
	*/
	
/*  Logical operators 
	(&&), //and
	(||),  //or
	(!) //not
*/

/* Unary operators (!, typeOf, -)
	An operator with only one operand 
     Typeof returns a string which holds the name of the type as a value.
		// Console.log(typeof false); // prints 'boolean'
*/

/* Ternary operator (a ? b : c)
*/